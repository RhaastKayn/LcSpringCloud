spring:
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        username: nacos # 账号
        password: nacos # 密码
      config:
        server-addr: localhost:8848
        file-extension: yaml
        prefix: shopping-mall-service
        group: DEFAULT_GROUP
        username: nacos # 账号
        password: nacos # 密码
